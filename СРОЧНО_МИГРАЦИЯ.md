# 🚨 СРОЧНАЯ МИГРАЦИЯ БАЗЫ ДАННЫХ

## ❌ ОШИБКА: `column addresses.street does not exist`

Нужно добавить колонку `street` в таблицу `addresses` в PostgreSQL на Railway!

---

## 🔧 КАК ИСПРАВИТЬ (2 способа)

### ✅ СПОСОБ 1: Через Railway Dashboard (ПРОЩЕ)

1. **Открой Railway Dashboard** → твой проект
2. Найди сервис **Postgres** (база данных)
3. Перейди на вкладку **Data** (или **Query**)
4. Вставь и **выполни** этот SQL:

```sql
-- Добавить колонку street
ALTER TABLE addresses ADD COLUMN IF NOT EXISTS street VARCHAR(200);

-- Сделать complex_id необязательным
ALTER TABLE addresses ALTER COLUMN complex_id DROP NOT NULL;
```

5. Нажми **Execute** (или **Run**)
6. ✅ Готово!

---

### ✅ СПОСОБ 2: Через psql (если есть доступ)

1. Открой Railway → Postgres → **Connect**
2. Скопируй **Database URL**
3. В терминале выполни:

```bash
psql "твоя_database_url" -f backend/migration_add_street.sql
```

---

## 🔄 ПОСЛЕ МИГРАЦИИ

1. **Перезапусти Backend** на Railway (или он перезапустится автоматически)
2. **Проверь** — создание заказа должно работать!

---

## ✅ ПРОВЕРКА

Выполни в Railway Postgres Query:

```sql
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'addresses';
```

Должно быть:
- `street` | `character varying` | `YES`
- `complex_id` | `integer` | `YES` (nullable)

---

## 🆘 ЕСЛИ НЕ ПОЛУЧАЕТСЯ

Напиши мне — я помогу!

